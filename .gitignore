node_modules
CLAUDE.md
.CLAUDE
apps/editor/dist
.env
.env.local

# tsc -b / vite build output — regenerated by `pnpm build`, never hand-edit
apps/editor/*.tsbuildinfo
apps/editor/vite.config.js
apps/editor/vite.config.d.ts
apps/editor/src/utils/awsPublishHandler.js
apps/editor/src/utils/awsPublishHandler.d.ts
apps/editor/src/utils/publishAuth.js
apps/editor/src/utils/publishAuth.d.ts

# tsc -b typecheck output for the Vercel functions (api/) — kept out of api/ so Vercel
# only ever sees the .ts sources there
apps/editor/.tsbuild

# npm/yarn lockfiles - this repo uses pnpm, and a stray lockfile in apps/editor makes
# Vercel detect the wrong package manager for the deploy Root Directory
package-lock.json
yarn.lock
